box: wercker/golang@1.2.0
services:
 - packrat386/riak-wercker@0.0.2
build:
 steps:
  - script:
      name: install-dep
      code: go get gopkg.in/check.v1 && go get code.google.com/p/gogoprotobuf/proto
  - script:
      name: test
      code: go test -v -tags 'riak' -check.v